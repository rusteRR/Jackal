#include "doctest.h"
#include "game.h"

using namespace jackal;

TEST_CASE("Incorrect moves") {
    /*Game game(jackal::game_type::DEFAULT);
    auto result = game.process_move("ship_move", 0, 6, 1);
    CHECK(result["response_type"] == "requests_error");
    result = game.process_move("pirate_move", 0, 6, 2);
    CHECK(result["response_type"] == "requests_error");*/
}